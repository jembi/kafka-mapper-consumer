{
  "private": true,
  "workspaces": {
    "packages": [
      "apps/kafka-mapper-consumer",
      "apps/kafka-mapper-microfrontend"
    ],
    "nohoist": [
      "**/react",
      "**/react/**",
      "**/react-dom",
      "**/react-dom/**"
    ]
  },
  "license": "MIT",
  "scripts": {
    "start": "yarn workspaces run start",
    "build": "yarn workspaces run build"
  }
}
