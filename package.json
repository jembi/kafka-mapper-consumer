{
  "name": "kafka-mapper-consumer",
  "version": "0.0.1",
  "license": "MIT",
  "main": "src/index.ts",
  "dependencies": {
    "fhirpath": "^2.14.6",
    "kafkajs": "2.0.2"
  },
  "devDependencies": {
    "@types/node": "18.7.6",
    "typescript": "4.7.4"
  },
  "scripts": {
    "start": "node dist/index.js",
    "build": "rm -r dist && tsc",
    "build:docker": "yarn build && docker build -t jembi/kafka-mapper-consumer ."
  }
}
